# LaBrute Environment Configuration
# Rename this file to .env and fill in your values
# Or set these as environment variables in Railway/Render
#
# IMPORTANT: Never commit .env to version control!

# Database Configuration
# For Railway/Render, DATABASE_URL is usually provided automatically
# Format: postgres://user:password@host:port/database
DATABASE_URL=postgres://user:password@localhost:5432/labrute

# Or use individual variables (if DATABASE_URL is not available)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=labrute
DB_USER=postgres
DB_PASS=

# Application Settings
APP_ENV=production
APP_DEBUG=false
APP_URL=https://your-app-url.railway.app

# Session Configuration (optional)
# SESSION_LIFETIME=604800
# SESSION_NAME=labrute_session

# Security (optional)
# PASSWORD_COST=12

